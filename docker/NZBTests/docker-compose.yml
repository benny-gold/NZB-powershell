version: '2'
services:
  sab:
    image: "sabnzbd/sabnzbd"
    ports:
         - 7770:8080
    volumes:
         - ./sab:/datadir
    cap_add:
      - SYS_ADMIN
      - DAC_READ_SEARCH
  sonarr:
    image: "linuxserver/sonarr"
    ports:
         - 7771:8989
    volumes:
         - ./sonarr:/config
    cap_add:
      - SYS_ADMIN
      - DAC_READ_SEARCH
  couchpotato:
    image: "linuxserver/couchpotato"
    ports:
         - 7772:5050
    volumes:
         - ./couch:/config
